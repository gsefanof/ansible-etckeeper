---
sudo: required
language: python
services:
  - docker

addons:
  hosts:
    - centos

install:
  - pip install -r requirements.txt

script:
  - python -V
  - molecule --version
  - ansible --version
  - molecule test -s default
